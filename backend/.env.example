# Orígenes CORS permitidos (separados por coma, sin espacios extra)
CORS_ORIGINS=http://localhost:3000

# Rutas a artefactos del modelo (opcional)
# MODEL_PATH=./models/tennis_model.pkl
# PLAYER_PROFILES_PATH=./models/player_profiles.pkl

# logging: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Solo para desarrollo sin modelo entrenado (la API arranca en modo degradado)
# ALLOW_START_WITHOUT_MODEL=true

# Fase 7 (scripts/fetch_live_odds.py, scripts/settle_live_odds.py): cuotas
# en vivo para trackear CLV real hacia adelante. Key gratis en
# https://the-odds-api.com. No cubre ATP 250 (solo Grand Slam/1000/500).
# ODDS_API_KEY=tu_clave

# URL de la API real contra la que fetch_live_odds.py llama /players/search
# y /predict (default http://localhost:8000 si se omite)
# API_BASE_URL=http://localhost:8000
